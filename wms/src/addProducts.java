import java.util.*;

public class addProducts {
    /*
    public static void main(String[] args){
        database db = new database("products.txt");

        String productId = UUID.randomUUID().toString();
        
        String[] productNameArray = {"Fanta", "Coca cola", "Sprite", "7up", "Salt", "Soap", "Noodles", "Vasline", "Tooth paste"};
        String[] manufacturesArray = {"The coca cola company", "Transcom beverages ltd", "Square toiletries ltd", "Unilever Bangladesh ltd", "Reckitt Benckiser Bangladesh ltd"};
        String[] categoryArray = {"Beverages", "Toiletries", "Food"};
        String[] subCategoryArray = {"Soft drinks", "Salt", "Soap", "Noodles", "Vasline", "Tooth paste"};
        String[] priceArray = {"20", "30", "40", "50", "60", "70", "80", "90", "100"};
        String[] quantityArray = {"100", "200", "300", "400", "500", "600", "700", "800", "900", "1000"};

        //add products to the database using loop
        for (int i = 0; i < 100; i++){
            String productName = productNameArray[(int)(Math.random() * productNameArray.length)];
            String manufacture = manufacturesArray[(int)(Math.random() * manufacturesArray.length)];
            String category = categoryArray[(int)(Math.random() * categoryArray.length)];
            String subCategory = subCategoryArray[(int)(Math.random() * subCategoryArray.length)];
            String price = priceArray[(int)(Math.random() * priceArray.length)];
            String quantity = quantityArray[(int)(Math.random() * quantityArray.length)];

            String data = "productName=" + productName + ",manufacture=" + manufacture + ",category=" + category + ",subCategory=" + subCategory + ",price=" + price + ",quantity=" + quantity + ",productId=" + productId + "\n";
            db.add(data);
        }


    }
    */
}
